local config = {}

config.checkpermission = true
config.permissions = {
    "manager.permissao",
}

config.command = {
    name = "pneucds",
    permissions = {
        "manager.permissao"
    }
}

config.prop = "prop_rub_pile_04"

return config